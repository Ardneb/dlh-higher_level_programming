## 0-square.py
Write an empty class Square that defines a square
## 1-square.py
Write a class Square that defines a square by private instance attribute size
## 2-square.py
Write a class Square that defines a square by private instance attribute size. Size must be an integer, otherwise raise a TypeError exception with the message size must be an integer
if size is less than 0, raise a ValueError exception with the message size must be >= 0
## 3-square.py
Write a class Square that defines a square by private instance attribute size. Size must be an integer, otherwise raise a TypeError exception with the message size must be an integer if size is less than 0, raise a ValueError exception with the message size must be >= 0. Calculate the area of the square.
## 4-square.py
Write a class Square that defines a square by private instance attribute size. Size must be an integer, otherwise raise a TypeError exception with the message size must be an integer if size is less than 0, raise a ValueError exception with the message size must be >= 0. Calculate the area of the square. Use property and property setter.
## 5-square.py
Write a class Square that defines a square by private instance attribute size. Size must be an integer, otherwise raise a TypeError exception with the message size must be an integer if size is less than 0, raise a ValueError exception with the message size must be >= 0. Calculate the area of the square. Use property and property setter. Print in stdout the square with the character #.
## 6-square.py
Write a class Square that defines a square by private instance attribute size. Size must be an integer, otherwise raise a TypeError exception with the message size must be an integer if size is less than 0, raise a ValueError exception with the message size must be >= 0. Calculate the area of the square. Use property and property setter. Print in stdout the square with the character #. Position should be set using space - Don't fill in empty lines or lines with only spaces when position[1] > 0 